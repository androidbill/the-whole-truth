# Firebase web app config (safe to expose in client code).
# Copy to .env.local and fill in from Firebase console → Project settings → Your apps.
VITE_FB_API_KEY=
VITE_FB_AUTH_DOMAIN=
VITE_FB_PROJECT_ID=
VITE_FB_APP_ID=
